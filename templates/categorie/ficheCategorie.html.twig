{% extends 'base.html.twig' %}

{% block title %}Fiche categorie{% endblock %}

{% block body %}

<h1 class="text-center pt-2">Fiche categorie</h1>

<div class="row">



                <div class="card mb-3 container">
                <h4 class="card-header">{{laCategorie.libelle}}</h4>
                <div class="row">
                    <div class="col-2 d-flex align-item-center">
                    <img class="avatar mx-auto my-3" style="height:auto; max-width :200px; display:block;" src=" {{laCategorie.image}}" alt="Card image">

                    </div>
                    <div class="col">
                        <ul class="list-group list-group-flush">
                        <li class="list-group-item">{{laCategorie.description}}</li>
                        </ul>
                    
                    </div>
                </div>
                </div>

    <a href="{{path('categorie')}}" class="btn btn-success text-center" style="width:unset" >Revenir à la liste</a>

</div>

{% endblock %}
 